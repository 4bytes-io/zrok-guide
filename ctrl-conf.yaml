v: 4

admin:
  secrets:
  - <admin token>

endpoints:
  host: 0.0.0.0
  port: <zrok port>

# you need postgresql if you want multiple zrok controllers
# more details config file can be found here: https://github.com/openziti/zrok/blob/main/etc/ctrl.yml
store:
  path: zrok.db
  type: sqlite3

ziti:
  api_endpoint: <ziti controller url>
  user_name: <ziti controller admin>
  passwiord: <ziti controller password>